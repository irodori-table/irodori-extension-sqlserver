name: CI

on:
  push:
  pull_request:

jobs:
  extension:
    uses: hjosugi/irodori-kit/.github/workflows/extension-ci.yml@v0.6.5
    with:
      manifest-root: .
      package-command: make package
      duckdb-backed: false
